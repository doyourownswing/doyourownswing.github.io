import theme from "common/theme";

const quoteAttributionStyles = {
  quoteAttributionContainer: { display: "flex", alignItems: "center" },
  avatar: {
    marginRight: "12px",
    backgroundColor: theme.palette.icon.avatar,
  },
  avatarImage: { height: "40px", width: "40px" },
};

export default quoteAttributionStyles;
